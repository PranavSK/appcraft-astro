export const AboutCode = () => (
  <>
    <br />
    You can access data from other nodes via the{' '}
    <code class='whitespace-nowrap'>get(id, key)</code> function and set data
    via the <code class='whitespace-nowrap'>set(id, key, value)</code> function.
  </>
)
